import 'package:flutter/material.dart';

class StudGridView {
  final String title;
  Icon icon;
  StudGridView({
    required this.title,
    required this.icon,
  });
}

class AdminDrawer {
  final String title;
  Icon icon;
  AdminDrawer({
    required this.title,
    required this.icon,
  });
}
